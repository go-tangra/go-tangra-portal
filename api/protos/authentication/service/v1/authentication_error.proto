syntax = "proto3";

package authentication.service.v1;

import "errors/errors.proto";

// AuthenticationErrorReason is the authentication system error definition
enum AuthenticationErrorReason {
    option (errors.default_code) = 500;

    // 400
    BAD_REQUEST = 0 [(errors.code) = 400]; // Bad request
    INVALID_GRANT_TYPE = 1 [(errors.code) = 400];// Invalid grant type
    INVALID_USERID = 2 [(errors.code) = 400];// Invalid user ID
    INVALID_TOKEN = 3 [(errors.code) = 400];// Invalid token
    INVALID_PASSWORD = 4 [(errors.code) = 400];// Invalid password

    // 401
    UNAUTHORIZED = 100 [(errors.code) = 401]; // Unauthorized
    USER_FREEZE = 101 [(errors.code) = 401]; // User frozen
    INCORRECT_PASSWORD = 102 [(errors.code) = 401]; // Incorrect password
    INCORRECT_APP_SECRET = 103 [(errors.code) = 401];// Incorrect app secret
    INCORRECT_ACCESS_TOKEN = 104 [(errors.code) = 401];// Incorrect access token
    INCORRECT_REFRESH_TOKEN = 105 [(errors.code) = 401];// Incorrect refresh token
    TOKEN_EXPIRED = 106 [(errors.code) = 401];// Token expired
    TOKEN_NOT_EXIST = 107 [(errors.code) = 401];// Token does not exist

    // 402
    PAYMENT_REQUIRED = 200 [(errors.code) = 402]; // Payment required

    // 403
    FORBIDDEN = 300 [(errors.code) = 403]; // Forbidden

    // 404
    NOT_FOUND = 400 [(errors.code) = 404]; // Resource not found
    USER_NOT_FOUND = 401 [(errors.code) = 404]; // User not found

    // 405
    METHOD_NOT_ALLOWED = 500 [(errors.code) = 405]; // Method not allowed

    // 406
    NOT_ACCEPTABLE = 600 [(errors.code) = 406]; // Not acceptable

    // 407
    PROXY_AUTHENTICATION_REQUIRED = 700 [(errors.code) = 407]; // Proxy authentication required

    // 408
    REQUEST_TIMEOUT = 800 [(errors.code) = 408]; // Request timeout

    // 409
    CONFLICT = 900 [(errors.code) = 409];                   // Conflict

    // 410
    GONE = 1000 [(errors.code) = 410];                       // Gone

    // 411
    LENGTH_REQUIRED = 1010 [(errors.code) = 411];            // Content-Length required

    // 412
    PRECONDITION_FAILED = 1020 [(errors.code) = 412];        // Precondition failed

    // 413
    PAYLOAD_TOO_LARGE = 1030 [(errors.code) = 413];          // Payload too large

    // 414
    URI_TOO_LONG = 1040 [(errors.code) = 414];               // URI too long

    // 415
    UNSUPPORTED_MEDIA_TYPE = 1050 [(errors.code) = 415];     // Unsupported media type

    // 416
    RANGE_NOT_SATISFIABLE = 1060 [(errors.code) = 416];      // Range not satisfiable

    // 417
    EXPECTATION_FAILED = 1070 [(errors.code) = 417];         // Expectation failed

    // 418
    IM_A_TEAPOT = 1080 [(errors.code) = 418];                // I'm a teapot (RFC 2324)

    // 421
    MISDIRECTED_REQUEST = 1090 [(errors.code) = 421];        // Misdirected request

    // 422
    UNPROCESSABLE_ENTITY = 1100 [(errors.code) = 422];       // Unprocessable entity

    // 423
    LOCKED = 1110 [(errors.code) = 423];                     // Locked

    // 424
    FAILED_DEPENDENCY = 1120 [(errors.code) = 424];          // Failed dependency

    // 425
    TOO_EARLY = 1130 [(errors.code) = 425];                  // Too early

    // 426
    UPGRADE_REQUIRED = 1140 [(errors.code) = 426];           // Upgrade required

    // 428
    PRECONDITION_REQUIRED = 1150 [(errors.code) = 428];      // Precondition required

    // 429
    TOO_MANY_REQUESTS = 1160 [(errors.code) = 429];          // Too many requests

    // 431
    REQUEST_HEADER_FIELDS_TOO_LARGE = 1170 [(errors.code) = 431]; // Request header fields too large

    // 451
    UNAVAILABLE_FOR_LEGAL_REASONS = 1180 [(errors.code) = 451]; // Unavailable for legal reasons


    // 500
    INTERNAL_SERVER_ERROR = 2000  [(errors.code) = 500];        // Internal server error

    // 501
    NOT_IMPLEMENTED = 2100 [(errors.code) = 501];              // Not implemented

    // 502
    BAD_GATEWAY = 2200 [(errors.code) = 502];                  // Bad gateway

    // 503
    SERVICE_UNAVAILABLE = 2300 [(errors.code) = 503];          // Service unavailable

    // 504
    GATEWAY_TIMEOUT = 2400 [(errors.code) = 504];              // Gateway timeout

    // 505
    HTTP_VERSION_NOT_SUPPORTED = 2500 [(errors.code) = 505];   // HTTP version not supported

    // 506
    VARIANT_ALSO_NEGOTIATES = 2600 [(errors.code) = 506];      // Variant also negotiates

    // 507
    INSUFFICIENT_STORAGE = 2700 [(errors.code) = 507];         // Insufficient storage

    // 508
    LOOP_DETECTED = 2800 [(errors.code) = 508];                // Loop detected

    // 510
    NOT_EXTENDED = 2900 [(errors.code) = 510];                 // Not extended

    // 511
    NETWORK_AUTHENTICATION_REQUIRED = 3000 [(errors.code) = 511]; // Network authentication required


    // Non-standard status codes

    // 598
    NETWORK_READ_TIMEOUT_ERROR = 3100 [(errors.code) = 598];   // Network read timeout

    // 599
    NETWORK_CONNECT_TIMEOUT_ERROR = 3200 [(errors.code) = 599]; // Network connect timeout

    // MFA-specific errors
    MFA_CHALLENGE_REQUIRED = 4000 [(errors.code) = 403];      // MFA verification required to complete login
    MFA_TOKEN_EXPIRED = 4001 [(errors.code) = 422];           // MFA session token has expired
    MFA_TOKEN_INVALID = 4002 [(errors.code) = 422];           // MFA session token is invalid
    MFA_VERIFICATION_FAILED = 4003 [(errors.code) = 422];     // MFA code verification failed
    MFA_ALREADY_ENROLLED = 4004 [(errors.code) = 409];        // MFA method already enrolled
    MFA_NOT_ENROLLED = 4005 [(errors.code) = 404];            // No MFA method enrolled
}
