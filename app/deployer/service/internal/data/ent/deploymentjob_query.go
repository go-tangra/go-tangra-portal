// Code generated by ent, DO NOT EDIT.

package ent

import (
	"context"
	"database/sql/driver"
	"errors"
	"fmt"
	"github.com/go-tangra/go-tangra-portal/app/deployer/service/internal/data/ent/deploymenthistory"
	"github.com/go-tangra/go-tangra-portal/app/deployer/service/internal/data/ent/deploymentjob"
	"github.com/go-tangra/go-tangra-portal/app/deployer/service/internal/data/ent/deploymenttarget"
	"github.com/go-tangra/go-tangra-portal/app/deployer/service/internal/data/ent/predicate"
	"github.com/go-tangra/go-tangra-portal/app/deployer/service/internal/data/ent/targetconfiguration"
	"math"

	"entgo.io/ent"
	"entgo.io/ent/dialect"
	"entgo.io/ent/dialect/sql"
	"entgo.io/ent/dialect/sql/sqlgraph"
	"entgo.io/ent/schema/field"
)

// DeploymentJobQuery is the builder for querying DeploymentJob entities.
type DeploymentJobQuery struct {
	config
	ctx                     *QueryContext
	order                   []deploymentjob.OrderOption
	inters                  []Interceptor
	predicates              []predicate.DeploymentJob
	withDeploymentTarget    *DeploymentTargetQuery
	withTargetConfiguration *TargetConfigurationQuery
	withParentJob           *DeploymentJobQuery
	withChildJobs           *DeploymentJobQuery
	withHistory             *DeploymentHistoryQuery
	modifiers               []func(*sql.Selector)
	// intermediate query (i.e. traversal path).
	sql  *sql.Selector
	path func(context.Context) (*sql.Selector, error)
}

// Where adds a new predicate for the DeploymentJobQuery builder.
func (_q *DeploymentJobQuery) Where(ps ...predicate.DeploymentJob) *DeploymentJobQuery {
	_q.predicates = append(_q.predicates, ps...)
	return _q
}

// Limit the number of records to be returned by this query.
func (_q *DeploymentJobQuery) Limit(limit int) *DeploymentJobQuery {
	_q.ctx.Limit = &limit
	return _q
}

// Offset to start from.
func (_q *DeploymentJobQuery) Offset(offset int) *DeploymentJobQuery {
	_q.ctx.Offset = &offset
	return _q
}

// Unique configures the query builder to filter duplicate records on query.
// By default, unique is set to true, and can be disabled using this method.
func (_q *DeploymentJobQuery) Unique(unique bool) *DeploymentJobQuery {
	_q.ctx.Unique = &unique
	return _q
}

// Order specifies how the records should be ordered.
func (_q *DeploymentJobQuery) Order(o ...deploymentjob.OrderOption) *DeploymentJobQuery {
	_q.order = append(_q.order, o...)
	return _q
}

// QueryDeploymentTarget chains the current query on the "deployment_target" edge.
func (_q *DeploymentJobQuery) QueryDeploymentTarget() *DeploymentTargetQuery {
	query := (&DeploymentTargetClient{config: _q.config}).Query()
	query.path = func(ctx context.Context) (fromU *sql.Selector, err error) {
		if err := _q.prepareQuery(ctx); err != nil {
			return nil, err
		}
		selector := _q.sqlQuery(ctx)
		if err := selector.Err(); err != nil {
			return nil, err
		}
		step := sqlgraph.NewStep(
			sqlgraph.From(deploymentjob.Table, deploymentjob.FieldID, selector),
			sqlgraph.To(deploymenttarget.Table, deploymenttarget.FieldID),
			sqlgraph.Edge(sqlgraph.M2O, true, deploymentjob.DeploymentTargetTable, deploymentjob.DeploymentTargetColumn),
		)
		fromU = sqlgraph.SetNeighbors(_q.driver.Dialect(), step)
		return fromU, nil
	}
	return query
}

// QueryTargetConfiguration chains the current query on the "target_configuration" edge.
func (_q *DeploymentJobQuery) QueryTargetConfiguration() *TargetConfigurationQuery {
	query := (&TargetConfigurationClient{config: _q.config}).Query()
	query.path = func(ctx context.Context) (fromU *sql.Selector, err error) {
		if err := _q.prepareQuery(ctx); err != nil {
			return nil, err
		}
		selector := _q.sqlQuery(ctx)
		if err := selector.Err(); err != nil {
			return nil, err
		}
		step := sqlgraph.NewStep(
			sqlgraph.From(deploymentjob.Table, deploymentjob.FieldID, selector),
			sqlgraph.To(targetconfiguration.Table, targetconfiguration.FieldID),
			sqlgraph.Edge(sqlgraph.M2O, true, deploymentjob.TargetConfigurationTable, deploymentjob.TargetConfigurationColumn),
		)
		fromU = sqlgraph.SetNeighbors(_q.driver.Dialect(), step)
		return fromU, nil
	}
	return query
}

// QueryParentJob chains the current query on the "parent_job" edge.
func (_q *DeploymentJobQuery) QueryParentJob() *DeploymentJobQuery {
	query := (&DeploymentJobClient{config: _q.config}).Query()
	query.path = func(ctx context.Context) (fromU *sql.Selector, err error) {
		if err := _q.prepareQuery(ctx); err != nil {
			return nil, err
		}
		selector := _q.sqlQuery(ctx)
		if err := selector.Err(); err != nil {
			return nil, err
		}
		step := sqlgraph.NewStep(
			sqlgraph.From(deploymentjob.Table, deploymentjob.FieldID, selector),
			sqlgraph.To(deploymentjob.Table, deploymentjob.FieldID),
			sqlgraph.Edge(sqlgraph.M2O, true, deploymentjob.ParentJobTable, deploymentjob.ParentJobColumn),
		)
		fromU = sqlgraph.SetNeighbors(_q.driver.Dialect(), step)
		return fromU, nil
	}
	return query
}

// QueryChildJobs chains the current query on the "child_jobs" edge.
func (_q *DeploymentJobQuery) QueryChildJobs() *DeploymentJobQuery {
	query := (&DeploymentJobClient{config: _q.config}).Query()
	query.path = func(ctx context.Context) (fromU *sql.Selector, err error) {
		if err := _q.prepareQuery(ctx); err != nil {
			return nil, err
		}
		selector := _q.sqlQuery(ctx)
		if err := selector.Err(); err != nil {
			return nil, err
		}
		step := sqlgraph.NewStep(
			sqlgraph.From(deploymentjob.Table, deploymentjob.FieldID, selector),
			sqlgraph.To(deploymentjob.Table, deploymentjob.FieldID),
			sqlgraph.Edge(sqlgraph.O2M, false, deploymentjob.ChildJobsTable, deploymentjob.ChildJobsColumn),
		)
		fromU = sqlgraph.SetNeighbors(_q.driver.Dialect(), step)
		return fromU, nil
	}
	return query
}

// QueryHistory chains the current query on the "history" edge.
func (_q *DeploymentJobQuery) QueryHistory() *DeploymentHistoryQuery {
	query := (&DeploymentHistoryClient{config: _q.config}).Query()
	query.path = func(ctx context.Context) (fromU *sql.Selector, err error) {
		if err := _q.prepareQuery(ctx); err != nil {
			return nil, err
		}
		selector := _q.sqlQuery(ctx)
		if err := selector.Err(); err != nil {
			return nil, err
		}
		step := sqlgraph.NewStep(
			sqlgraph.From(deploymentjob.Table, deploymentjob.FieldID, selector),
			sqlgraph.To(deploymenthistory.Table, deploymenthistory.FieldID),
			sqlgraph.Edge(sqlgraph.O2M, false, deploymentjob.HistoryTable, deploymentjob.HistoryColumn),
		)
		fromU = sqlgraph.SetNeighbors(_q.driver.Dialect(), step)
		return fromU, nil
	}
	return query
}

// First returns the first DeploymentJob entity from the query.
// Returns a *NotFoundError when no DeploymentJob was found.
func (_q *DeploymentJobQuery) First(ctx context.Context) (*DeploymentJob, error) {
	nodes, err := _q.Limit(1).All(setContextOp(ctx, _q.ctx, ent.OpQueryFirst))
	if err != nil {
		return nil, err
	}
	if len(nodes) == 0 {
		return nil, &NotFoundError{deploymentjob.Label}
	}
	return nodes[0], nil
}

// FirstX is like First, but panics if an error occurs.
func (_q *DeploymentJobQuery) FirstX(ctx context.Context) *DeploymentJob {
	node, err := _q.First(ctx)
	if err != nil && !IsNotFound(err) {
		panic(err)
	}
	return node
}

// FirstID returns the first DeploymentJob ID from the query.
// Returns a *NotFoundError when no DeploymentJob ID was found.
func (_q *DeploymentJobQuery) FirstID(ctx context.Context) (id string, err error) {
	var ids []string
	if ids, err = _q.Limit(1).IDs(setContextOp(ctx, _q.ctx, ent.OpQueryFirstID)); err != nil {
		return
	}
	if len(ids) == 0 {
		err = &NotFoundError{deploymentjob.Label}
		return
	}
	return ids[0], nil
}

// FirstIDX is like FirstID, but panics if an error occurs.
func (_q *DeploymentJobQuery) FirstIDX(ctx context.Context) string {
	id, err := _q.FirstID(ctx)
	if err != nil && !IsNotFound(err) {
		panic(err)
	}
	return id
}

// Only returns a single DeploymentJob entity found by the query, ensuring it only returns one.
// Returns a *NotSingularError when more than one DeploymentJob entity is found.
// Returns a *NotFoundError when no DeploymentJob entities are found.
func (_q *DeploymentJobQuery) Only(ctx context.Context) (*DeploymentJob, error) {
	nodes, err := _q.Limit(2).All(setContextOp(ctx, _q.ctx, ent.OpQueryOnly))
	if err != nil {
		return nil, err
	}
	switch len(nodes) {
	case 1:
		return nodes[0], nil
	case 0:
		return nil, &NotFoundError{deploymentjob.Label}
	default:
		return nil, &NotSingularError{deploymentjob.Label}
	}
}

// OnlyX is like Only, but panics if an error occurs.
func (_q *DeploymentJobQuery) OnlyX(ctx context.Context) *DeploymentJob {
	node, err := _q.Only(ctx)
	if err != nil {
		panic(err)
	}
	return node
}

// OnlyID is like Only, but returns the only DeploymentJob ID in the query.
// Returns a *NotSingularError when more than one DeploymentJob ID is found.
// Returns a *NotFoundError when no entities are found.
func (_q *DeploymentJobQuery) OnlyID(ctx context.Context) (id string, err error) {
	var ids []string
	if ids, err = _q.Limit(2).IDs(setContextOp(ctx, _q.ctx, ent.OpQueryOnlyID)); err != nil {
		return
	}
	switch len(ids) {
	case 1:
		id = ids[0]
	case 0:
		err = &NotFoundError{deploymentjob.Label}
	default:
		err = &NotSingularError{deploymentjob.Label}
	}
	return
}

// OnlyIDX is like OnlyID, but panics if an error occurs.
func (_q *DeploymentJobQuery) OnlyIDX(ctx context.Context) string {
	id, err := _q.OnlyID(ctx)
	if err != nil {
		panic(err)
	}
	return id
}

// All executes the query and returns a list of DeploymentJobs.
func (_q *DeploymentJobQuery) All(ctx context.Context) ([]*DeploymentJob, error) {
	ctx = setContextOp(ctx, _q.ctx, ent.OpQueryAll)
	if err := _q.prepareQuery(ctx); err != nil {
		return nil, err
	}
	qr := querierAll[[]*DeploymentJob, *DeploymentJobQuery]()
	return withInterceptors[[]*DeploymentJob](ctx, _q, qr, _q.inters)
}

// AllX is like All, but panics if an error occurs.
func (_q *DeploymentJobQuery) AllX(ctx context.Context) []*DeploymentJob {
	nodes, err := _q.All(ctx)
	if err != nil {
		panic(err)
	}
	return nodes
}

// IDs executes the query and returns a list of DeploymentJob IDs.
func (_q *DeploymentJobQuery) IDs(ctx context.Context) (ids []string, err error) {
	if _q.ctx.Unique == nil && _q.path != nil {
		_q.Unique(true)
	}
	ctx = setContextOp(ctx, _q.ctx, ent.OpQueryIDs)
	if err = _q.Select(deploymentjob.FieldID).Scan(ctx, &ids); err != nil {
		return nil, err
	}
	return ids, nil
}

// IDsX is like IDs, but panics if an error occurs.
func (_q *DeploymentJobQuery) IDsX(ctx context.Context) []string {
	ids, err := _q.IDs(ctx)
	if err != nil {
		panic(err)
	}
	return ids
}

// Count returns the count of the given query.
func (_q *DeploymentJobQuery) Count(ctx context.Context) (int, error) {
	ctx = setContextOp(ctx, _q.ctx, ent.OpQueryCount)
	if err := _q.prepareQuery(ctx); err != nil {
		return 0, err
	}
	return withInterceptors[int](ctx, _q, querierCount[*DeploymentJobQuery](), _q.inters)
}

// CountX is like Count, but panics if an error occurs.
func (_q *DeploymentJobQuery) CountX(ctx context.Context) int {
	count, err := _q.Count(ctx)
	if err != nil {
		panic(err)
	}
	return count
}

// Exist returns true if the query has elements in the graph.
func (_q *DeploymentJobQuery) Exist(ctx context.Context) (bool, error) {
	ctx = setContextOp(ctx, _q.ctx, ent.OpQueryExist)
	switch _, err := _q.FirstID(ctx); {
	case IsNotFound(err):
		return false, nil
	case err != nil:
		return false, fmt.Errorf("ent: check existence: %w", err)
	default:
		return true, nil
	}
}

// ExistX is like Exist, but panics if an error occurs.
func (_q *DeploymentJobQuery) ExistX(ctx context.Context) bool {
	exist, err := _q.Exist(ctx)
	if err != nil {
		panic(err)
	}
	return exist
}

// Clone returns a duplicate of the DeploymentJobQuery builder, including all associated steps. It can be
// used to prepare common query builders and use them differently after the clone is made.
func (_q *DeploymentJobQuery) Clone() *DeploymentJobQuery {
	if _q == nil {
		return nil
	}
	return &DeploymentJobQuery{
		config:                  _q.config,
		ctx:                     _q.ctx.Clone(),
		order:                   append([]deploymentjob.OrderOption{}, _q.order...),
		inters:                  append([]Interceptor{}, _q.inters...),
		predicates:              append([]predicate.DeploymentJob{}, _q.predicates...),
		withDeploymentTarget:    _q.withDeploymentTarget.Clone(),
		withTargetConfiguration: _q.withTargetConfiguration.Clone(),
		withParentJob:           _q.withParentJob.Clone(),
		withChildJobs:           _q.withChildJobs.Clone(),
		withHistory:             _q.withHistory.Clone(),
		// clone intermediate query.
		sql:       _q.sql.Clone(),
		path:      _q.path,
		modifiers: append([]func(*sql.Selector){}, _q.modifiers...),
	}
}

// WithDeploymentTarget tells the query-builder to eager-load the nodes that are connected to
// the "deployment_target" edge. The optional arguments are used to configure the query builder of the edge.
func (_q *DeploymentJobQuery) WithDeploymentTarget(opts ...func(*DeploymentTargetQuery)) *DeploymentJobQuery {
	query := (&DeploymentTargetClient{config: _q.config}).Query()
	for _, opt := range opts {
		opt(query)
	}
	_q.withDeploymentTarget = query
	return _q
}

// WithTargetConfiguration tells the query-builder to eager-load the nodes that are connected to
// the "target_configuration" edge. The optional arguments are used to configure the query builder of the edge.
func (_q *DeploymentJobQuery) WithTargetConfiguration(opts ...func(*TargetConfigurationQuery)) *DeploymentJobQuery {
	query := (&TargetConfigurationClient{config: _q.config}).Query()
	for _, opt := range opts {
		opt(query)
	}
	_q.withTargetConfiguration = query
	return _q
}

// WithParentJob tells the query-builder to eager-load the nodes that are connected to
// the "parent_job" edge. The optional arguments are used to configure the query builder of the edge.
func (_q *DeploymentJobQuery) WithParentJob(opts ...func(*DeploymentJobQuery)) *DeploymentJobQuery {
	query := (&DeploymentJobClient{config: _q.config}).Query()
	for _, opt := range opts {
		opt(query)
	}
	_q.withParentJob = query
	return _q
}

// WithChildJobs tells the query-builder to eager-load the nodes that are connected to
// the "child_jobs" edge. The optional arguments are used to configure the query builder of the edge.
func (_q *DeploymentJobQuery) WithChildJobs(opts ...func(*DeploymentJobQuery)) *DeploymentJobQuery {
	query := (&DeploymentJobClient{config: _q.config}).Query()
	for _, opt := range opts {
		opt(query)
	}
	_q.withChildJobs = query
	return _q
}

// WithHistory tells the query-builder to eager-load the nodes that are connected to
// the "history" edge. The optional arguments are used to configure the query builder of the edge.
func (_q *DeploymentJobQuery) WithHistory(opts ...func(*DeploymentHistoryQuery)) *DeploymentJobQuery {
	query := (&DeploymentHistoryClient{config: _q.config}).Query()
	for _, opt := range opts {
		opt(query)
	}
	_q.withHistory = query
	return _q
}

// GroupBy is used to group vertices by one or more fields/columns.
// It is often used with aggregate functions, like: count, max, mean, min, sum.
//
// Example:
//
//	var v []struct {
//		CreateBy uint32 `json:"create_by,omitempty"`
//		Count int `json:"count,omitempty"`
//	}
//
//	client.DeploymentJob.Query().
//		GroupBy(deploymentjob.FieldCreateBy).
//		Aggregate(ent.Count()).
//		Scan(ctx, &v)
func (_q *DeploymentJobQuery) GroupBy(field string, fields ...string) *DeploymentJobGroupBy {
	_q.ctx.Fields = append([]string{field}, fields...)
	grbuild := &DeploymentJobGroupBy{build: _q}
	grbuild.flds = &_q.ctx.Fields
	grbuild.label = deploymentjob.Label
	grbuild.scan = grbuild.Scan
	return grbuild
}

// Select allows the selection one or more fields/columns for the given query,
// instead of selecting all fields in the entity.
//
// Example:
//
//	var v []struct {
//		CreateBy uint32 `json:"create_by,omitempty"`
//	}
//
//	client.DeploymentJob.Query().
//		Select(deploymentjob.FieldCreateBy).
//		Scan(ctx, &v)
func (_q *DeploymentJobQuery) Select(fields ...string) *DeploymentJobSelect {
	_q.ctx.Fields = append(_q.ctx.Fields, fields...)
	sbuild := &DeploymentJobSelect{DeploymentJobQuery: _q}
	sbuild.label = deploymentjob.Label
	sbuild.flds, sbuild.scan = &_q.ctx.Fields, sbuild.Scan
	return sbuild
}

// Aggregate returns a DeploymentJobSelect configured with the given aggregations.
func (_q *DeploymentJobQuery) Aggregate(fns ...AggregateFunc) *DeploymentJobSelect {
	return _q.Select().Aggregate(fns...)
}

func (_q *DeploymentJobQuery) prepareQuery(ctx context.Context) error {
	for _, inter := range _q.inters {
		if inter == nil {
			return fmt.Errorf("ent: uninitialized interceptor (forgotten import ent/runtime?)")
		}
		if trv, ok := inter.(Traverser); ok {
			if err := trv.Traverse(ctx, _q); err != nil {
				return err
			}
		}
	}
	for _, f := range _q.ctx.Fields {
		if !deploymentjob.ValidColumn(f) {
			return &ValidationError{Name: f, err: fmt.Errorf("ent: invalid field %q for query", f)}
		}
	}
	if _q.path != nil {
		prev, err := _q.path(ctx)
		if err != nil {
			return err
		}
		_q.sql = prev
	}
	if deploymentjob.Policy == nil {
		return errors.New("ent: uninitialized deploymentjob.Policy (forgotten import ent/runtime?)")
	}
	if err := deploymentjob.Policy.EvalQuery(ctx, _q); err != nil {
		return err
	}
	return nil
}

func (_q *DeploymentJobQuery) sqlAll(ctx context.Context, hooks ...queryHook) ([]*DeploymentJob, error) {
	var (
		nodes       = []*DeploymentJob{}
		_spec       = _q.querySpec()
		loadedTypes = [5]bool{
			_q.withDeploymentTarget != nil,
			_q.withTargetConfiguration != nil,
			_q.withParentJob != nil,
			_q.withChildJobs != nil,
			_q.withHistory != nil,
		}
	)
	_spec.ScanValues = func(columns []string) ([]any, error) {
		return (*DeploymentJob).scanValues(nil, columns)
	}
	_spec.Assign = func(columns []string, values []any) error {
		node := &DeploymentJob{config: _q.config}
		nodes = append(nodes, node)
		node.Edges.loadedTypes = loadedTypes
		return node.assignValues(columns, values)
	}
	if len(_q.modifiers) > 0 {
		_spec.Modifiers = _q.modifiers
	}
	for i := range hooks {
		hooks[i](ctx, _spec)
	}
	if err := sqlgraph.QueryNodes(ctx, _q.driver, _spec); err != nil {
		return nil, err
	}
	if len(nodes) == 0 {
		return nodes, nil
	}
	if query := _q.withDeploymentTarget; query != nil {
		if err := _q.loadDeploymentTarget(ctx, query, nodes, nil,
			func(n *DeploymentJob, e *DeploymentTarget) { n.Edges.DeploymentTarget = e }); err != nil {
			return nil, err
		}
	}
	if query := _q.withTargetConfiguration; query != nil {
		if err := _q.loadTargetConfiguration(ctx, query, nodes, nil,
			func(n *DeploymentJob, e *TargetConfiguration) { n.Edges.TargetConfiguration = e }); err != nil {
			return nil, err
		}
	}
	if query := _q.withParentJob; query != nil {
		if err := _q.loadParentJob(ctx, query, nodes, nil,
			func(n *DeploymentJob, e *DeploymentJob) { n.Edges.ParentJob = e }); err != nil {
			return nil, err
		}
	}
	if query := _q.withChildJobs; query != nil {
		if err := _q.loadChildJobs(ctx, query, nodes,
			func(n *DeploymentJob) { n.Edges.ChildJobs = []*DeploymentJob{} },
			func(n *DeploymentJob, e *DeploymentJob) { n.Edges.ChildJobs = append(n.Edges.ChildJobs, e) }); err != nil {
			return nil, err
		}
	}
	if query := _q.withHistory; query != nil {
		if err := _q.loadHistory(ctx, query, nodes,
			func(n *DeploymentJob) { n.Edges.History = []*DeploymentHistory{} },
			func(n *DeploymentJob, e *DeploymentHistory) { n.Edges.History = append(n.Edges.History, e) }); err != nil {
			return nil, err
		}
	}
	return nodes, nil
}

func (_q *DeploymentJobQuery) loadDeploymentTarget(ctx context.Context, query *DeploymentTargetQuery, nodes []*DeploymentJob, init func(*DeploymentJob), assign func(*DeploymentJob, *DeploymentTarget)) error {
	ids := make([]string, 0, len(nodes))
	nodeids := make(map[string][]*DeploymentJob)
	for i := range nodes {
		if nodes[i].DeploymentTargetID == nil {
			continue
		}
		fk := *nodes[i].DeploymentTargetID
		if _, ok := nodeids[fk]; !ok {
			ids = append(ids, fk)
		}
		nodeids[fk] = append(nodeids[fk], nodes[i])
	}
	if len(ids) == 0 {
		return nil
	}
	query.Where(deploymenttarget.IDIn(ids...))
	neighbors, err := query.All(ctx)
	if err != nil {
		return err
	}
	for _, n := range neighbors {
		nodes, ok := nodeids[n.ID]
		if !ok {
			return fmt.Errorf(`unexpected foreign-key "deployment_target_id" returned %v`, n.ID)
		}
		for i := range nodes {
			assign(nodes[i], n)
		}
	}
	return nil
}
func (_q *DeploymentJobQuery) loadTargetConfiguration(ctx context.Context, query *TargetConfigurationQuery, nodes []*DeploymentJob, init func(*DeploymentJob), assign func(*DeploymentJob, *TargetConfiguration)) error {
	ids := make([]string, 0, len(nodes))
	nodeids := make(map[string][]*DeploymentJob)
	for i := range nodes {
		if nodes[i].TargetConfigurationID == nil {
			continue
		}
		fk := *nodes[i].TargetConfigurationID
		if _, ok := nodeids[fk]; !ok {
			ids = append(ids, fk)
		}
		nodeids[fk] = append(nodeids[fk], nodes[i])
	}
	if len(ids) == 0 {
		return nil
	}
	query.Where(targetconfiguration.IDIn(ids...))
	neighbors, err := query.All(ctx)
	if err != nil {
		return err
	}
	for _, n := range neighbors {
		nodes, ok := nodeids[n.ID]
		if !ok {
			return fmt.Errorf(`unexpected foreign-key "target_configuration_id" returned %v`, n.ID)
		}
		for i := range nodes {
			assign(nodes[i], n)
		}
	}
	return nil
}
func (_q *DeploymentJobQuery) loadParentJob(ctx context.Context, query *DeploymentJobQuery, nodes []*DeploymentJob, init func(*DeploymentJob), assign func(*DeploymentJob, *DeploymentJob)) error {
	ids := make([]string, 0, len(nodes))
	nodeids := make(map[string][]*DeploymentJob)
	for i := range nodes {
		if nodes[i].ParentJobID == nil {
			continue
		}
		fk := *nodes[i].ParentJobID
		if _, ok := nodeids[fk]; !ok {
			ids = append(ids, fk)
		}
		nodeids[fk] = append(nodeids[fk], nodes[i])
	}
	if len(ids) == 0 {
		return nil
	}
	query.Where(deploymentjob.IDIn(ids...))
	neighbors, err := query.All(ctx)
	if err != nil {
		return err
	}
	for _, n := range neighbors {
		nodes, ok := nodeids[n.ID]
		if !ok {
			return fmt.Errorf(`unexpected foreign-key "parent_job_id" returned %v`, n.ID)
		}
		for i := range nodes {
			assign(nodes[i], n)
		}
	}
	return nil
}
func (_q *DeploymentJobQuery) loadChildJobs(ctx context.Context, query *DeploymentJobQuery, nodes []*DeploymentJob, init func(*DeploymentJob), assign func(*DeploymentJob, *DeploymentJob)) error {
	fks := make([]driver.Value, 0, len(nodes))
	nodeids := make(map[string]*DeploymentJob)
	for i := range nodes {
		fks = append(fks, nodes[i].ID)
		nodeids[nodes[i].ID] = nodes[i]
		if init != nil {
			init(nodes[i])
		}
	}
	if len(query.ctx.Fields) > 0 {
		query.ctx.AppendFieldOnce(deploymentjob.FieldParentJobID)
	}
	query.Where(predicate.DeploymentJob(func(s *sql.Selector) {
		s.Where(sql.InValues(s.C(deploymentjob.ChildJobsColumn), fks...))
	}))
	neighbors, err := query.All(ctx)
	if err != nil {
		return err
	}
	for _, n := range neighbors {
		fk := n.ParentJobID
		if fk == nil {
			return fmt.Errorf(`foreign-key "parent_job_id" is nil for node %v`, n.ID)
		}
		node, ok := nodeids[*fk]
		if !ok {
			return fmt.Errorf(`unexpected referenced foreign-key "parent_job_id" returned %v for node %v`, *fk, n.ID)
		}
		assign(node, n)
	}
	return nil
}
func (_q *DeploymentJobQuery) loadHistory(ctx context.Context, query *DeploymentHistoryQuery, nodes []*DeploymentJob, init func(*DeploymentJob), assign func(*DeploymentJob, *DeploymentHistory)) error {
	fks := make([]driver.Value, 0, len(nodes))
	nodeids := make(map[string]*DeploymentJob)
	for i := range nodes {
		fks = append(fks, nodes[i].ID)
		nodeids[nodes[i].ID] = nodes[i]
		if init != nil {
			init(nodes[i])
		}
	}
	if len(query.ctx.Fields) > 0 {
		query.ctx.AppendFieldOnce(deploymenthistory.FieldJobID)
	}
	query.Where(predicate.DeploymentHistory(func(s *sql.Selector) {
		s.Where(sql.InValues(s.C(deploymentjob.HistoryColumn), fks...))
	}))
	neighbors, err := query.All(ctx)
	if err != nil {
		return err
	}
	for _, n := range neighbors {
		fk := n.JobID
		node, ok := nodeids[fk]
		if !ok {
			return fmt.Errorf(`unexpected referenced foreign-key "job_id" returned %v for node %v`, fk, n.ID)
		}
		assign(node, n)
	}
	return nil
}

func (_q *DeploymentJobQuery) sqlCount(ctx context.Context) (int, error) {
	_spec := _q.querySpec()
	if len(_q.modifiers) > 0 {
		_spec.Modifiers = _q.modifiers
	}
	_spec.Node.Columns = _q.ctx.Fields
	if len(_q.ctx.Fields) > 0 {
		_spec.Unique = _q.ctx.Unique != nil && *_q.ctx.Unique
	}
	return sqlgraph.CountNodes(ctx, _q.driver, _spec)
}

func (_q *DeploymentJobQuery) querySpec() *sqlgraph.QuerySpec {
	_spec := sqlgraph.NewQuerySpec(deploymentjob.Table, deploymentjob.Columns, sqlgraph.NewFieldSpec(deploymentjob.FieldID, field.TypeString))
	_spec.From = _q.sql
	if unique := _q.ctx.Unique; unique != nil {
		_spec.Unique = *unique
	} else if _q.path != nil {
		_spec.Unique = true
	}
	if fields := _q.ctx.Fields; len(fields) > 0 {
		_spec.Node.Columns = make([]string, 0, len(fields))
		_spec.Node.Columns = append(_spec.Node.Columns, deploymentjob.FieldID)
		for i := range fields {
			if fields[i] != deploymentjob.FieldID {
				_spec.Node.Columns = append(_spec.Node.Columns, fields[i])
			}
		}
		if _q.withDeploymentTarget != nil {
			_spec.Node.AddColumnOnce(deploymentjob.FieldDeploymentTargetID)
		}
		if _q.withTargetConfiguration != nil {
			_spec.Node.AddColumnOnce(deploymentjob.FieldTargetConfigurationID)
		}
		if _q.withParentJob != nil {
			_spec.Node.AddColumnOnce(deploymentjob.FieldParentJobID)
		}
	}
	if ps := _q.predicates; len(ps) > 0 {
		_spec.Predicate = func(selector *sql.Selector) {
			for i := range ps {
				ps[i](selector)
			}
		}
	}
	if limit := _q.ctx.Limit; limit != nil {
		_spec.Limit = *limit
	}
	if offset := _q.ctx.Offset; offset != nil {
		_spec.Offset = *offset
	}
	if ps := _q.order; len(ps) > 0 {
		_spec.Order = func(selector *sql.Selector) {
			for i := range ps {
				ps[i](selector)
			}
		}
	}
	return _spec
}

func (_q *DeploymentJobQuery) sqlQuery(ctx context.Context) *sql.Selector {
	builder := sql.Dialect(_q.driver.Dialect())
	t1 := builder.Table(deploymentjob.Table)
	columns := _q.ctx.Fields
	if len(columns) == 0 {
		columns = deploymentjob.Columns
	}
	selector := builder.Select(t1.Columns(columns...)...).From(t1)
	if _q.sql != nil {
		selector = _q.sql
		selector.Select(selector.Columns(columns...)...)
	}
	if _q.ctx.Unique != nil && *_q.ctx.Unique {
		selector.Distinct()
	}
	for _, m := range _q.modifiers {
		m(selector)
	}
	for _, p := range _q.predicates {
		p(selector)
	}
	for _, p := range _q.order {
		p(selector)
	}
	if offset := _q.ctx.Offset; offset != nil {
		// limit is mandatory for offset clause. We start
		// with default value, and override it below if needed.
		selector.Offset(*offset).Limit(math.MaxInt32)
	}
	if limit := _q.ctx.Limit; limit != nil {
		selector.Limit(*limit)
	}
	return selector
}

// ForUpdate locks the selected rows against concurrent updates, and prevent them from being
// updated, deleted or "selected ... for update" by other sessions, until the transaction is
// either committed or rolled-back.
func (_q *DeploymentJobQuery) ForUpdate(opts ...sql.LockOption) *DeploymentJobQuery {
	if _q.driver.Dialect() == dialect.Postgres {
		_q.Unique(false)
	}
	_q.modifiers = append(_q.modifiers, func(s *sql.Selector) {
		s.ForUpdate(opts...)
	})
	return _q
}

// ForShare behaves similarly to ForUpdate, except that it acquires a shared mode lock
// on any rows that are read. Other sessions can read the rows, but cannot modify them
// until your transaction commits.
func (_q *DeploymentJobQuery) ForShare(opts ...sql.LockOption) *DeploymentJobQuery {
	if _q.driver.Dialect() == dialect.Postgres {
		_q.Unique(false)
	}
	_q.modifiers = append(_q.modifiers, func(s *sql.Selector) {
		s.ForShare(opts...)
	})
	return _q
}

// Modify adds a query modifier for attaching custom logic to queries.
func (_q *DeploymentJobQuery) Modify(modifiers ...func(s *sql.Selector)) *DeploymentJobSelect {
	_q.modifiers = append(_q.modifiers, modifiers...)
	return _q.Select()
}

// DeploymentJobGroupBy is the group-by builder for DeploymentJob entities.
type DeploymentJobGroupBy struct {
	selector
	build *DeploymentJobQuery
}

// Aggregate adds the given aggregation functions to the group-by query.
func (_g *DeploymentJobGroupBy) Aggregate(fns ...AggregateFunc) *DeploymentJobGroupBy {
	_g.fns = append(_g.fns, fns...)
	return _g
}

// Scan applies the selector query and scans the result into the given value.
func (_g *DeploymentJobGroupBy) Scan(ctx context.Context, v any) error {
	ctx = setContextOp(ctx, _g.build.ctx, ent.OpQueryGroupBy)
	if err := _g.build.prepareQuery(ctx); err != nil {
		return err
	}
	return scanWithInterceptors[*DeploymentJobQuery, *DeploymentJobGroupBy](ctx, _g.build, _g, _g.build.inters, v)
}

func (_g *DeploymentJobGroupBy) sqlScan(ctx context.Context, root *DeploymentJobQuery, v any) error {
	selector := root.sqlQuery(ctx).Select()
	aggregation := make([]string, 0, len(_g.fns))
	for _, fn := range _g.fns {
		aggregation = append(aggregation, fn(selector))
	}
	if len(selector.SelectedColumns()) == 0 {
		columns := make([]string, 0, len(*_g.flds)+len(_g.fns))
		for _, f := range *_g.flds {
			columns = append(columns, selector.C(f))
		}
		columns = append(columns, aggregation...)
		selector.Select(columns...)
	}
	selector.GroupBy(selector.Columns(*_g.flds...)...)
	if err := selector.Err(); err != nil {
		return err
	}
	rows := &sql.Rows{}
	query, args := selector.Query()
	if err := _g.build.driver.Query(ctx, query, args, rows); err != nil {
		return err
	}
	defer rows.Close()
	return sql.ScanSlice(rows, v)
}

// DeploymentJobSelect is the builder for selecting fields of DeploymentJob entities.
type DeploymentJobSelect struct {
	*DeploymentJobQuery
	selector
}

// Aggregate adds the given aggregation functions to the selector query.
func (_s *DeploymentJobSelect) Aggregate(fns ...AggregateFunc) *DeploymentJobSelect {
	_s.fns = append(_s.fns, fns...)
	return _s
}

// Scan applies the selector query and scans the result into the given value.
func (_s *DeploymentJobSelect) Scan(ctx context.Context, v any) error {
	ctx = setContextOp(ctx, _s.ctx, ent.OpQuerySelect)
	if err := _s.prepareQuery(ctx); err != nil {
		return err
	}
	return scanWithInterceptors[*DeploymentJobQuery, *DeploymentJobSelect](ctx, _s.DeploymentJobQuery, _s, _s.inters, v)
}

func (_s *DeploymentJobSelect) sqlScan(ctx context.Context, root *DeploymentJobQuery, v any) error {
	selector := root.sqlQuery(ctx)
	aggregation := make([]string, 0, len(_s.fns))
	for _, fn := range _s.fns {
		aggregation = append(aggregation, fn(selector))
	}
	switch n := len(*_s.selector.flds); {
	case n == 0 && len(aggregation) > 0:
		selector.Select(aggregation...)
	case n != 0 && len(aggregation) > 0:
		selector.AppendSelect(aggregation...)
	}
	rows := &sql.Rows{}
	query, args := selector.Query()
	if err := _s.driver.Query(ctx, query, args, rows); err != nil {
		return err
	}
	defer rows.Close()
	return sql.ScanSlice(rows, v)
}

// Modify adds a query modifier for attaching custom logic to queries.
func (_s *DeploymentJobSelect) Modify(modifiers ...func(s *sql.Selector)) *DeploymentJobSelect {
	_s.modifiers = append(_s.modifiers, modifiers...)
	return _s
}
