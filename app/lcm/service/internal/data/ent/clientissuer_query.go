// Code generated by ent, DO NOT EDIT.

package ent

import (
	"context"
	"fmt"
	"github.com/go-tangra/go-tangra-portal/app/lcm/service/internal/data/ent/clientissuer"
	"github.com/go-tangra/go-tangra-portal/app/lcm/service/internal/data/ent/issuer"
	"github.com/go-tangra/go-tangra-portal/app/lcm/service/internal/data/ent/lcmclient"
	"github.com/go-tangra/go-tangra-portal/app/lcm/service/internal/data/ent/predicate"
	"math"

	"entgo.io/ent"
	"entgo.io/ent/dialect"
	"entgo.io/ent/dialect/sql"
	"entgo.io/ent/dialect/sql/sqlgraph"
	"entgo.io/ent/schema/field"
)

// ClientIssuerQuery is the builder for querying ClientIssuer entities.
type ClientIssuerQuery struct {
	config
	ctx           *QueryContext
	order         []clientissuer.OrderOption
	inters        []Interceptor
	predicates    []predicate.ClientIssuer
	withIssuer    *IssuerQuery
	withLcmClient *LcmClientQuery
	withFKs       bool
	modifiers     []func(*sql.Selector)
	// intermediate query (i.e. traversal path).
	sql  *sql.Selector
	path func(context.Context) (*sql.Selector, error)
}

// Where adds a new predicate for the ClientIssuerQuery builder.
func (_q *ClientIssuerQuery) Where(ps ...predicate.ClientIssuer) *ClientIssuerQuery {
	_q.predicates = append(_q.predicates, ps...)
	return _q
}

// Limit the number of records to be returned by this query.
func (_q *ClientIssuerQuery) Limit(limit int) *ClientIssuerQuery {
	_q.ctx.Limit = &limit
	return _q
}

// Offset to start from.
func (_q *ClientIssuerQuery) Offset(offset int) *ClientIssuerQuery {
	_q.ctx.Offset = &offset
	return _q
}

// Unique configures the query builder to filter duplicate records on query.
// By default, unique is set to true, and can be disabled using this method.
func (_q *ClientIssuerQuery) Unique(unique bool) *ClientIssuerQuery {
	_q.ctx.Unique = &unique
	return _q
}

// Order specifies how the records should be ordered.
func (_q *ClientIssuerQuery) Order(o ...clientissuer.OrderOption) *ClientIssuerQuery {
	_q.order = append(_q.order, o...)
	return _q
}

// QueryIssuer chains the current query on the "issuer" edge.
func (_q *ClientIssuerQuery) QueryIssuer() *IssuerQuery {
	query := (&IssuerClient{config: _q.config}).Query()
	query.path = func(ctx context.Context) (fromU *sql.Selector, err error) {
		if err := _q.prepareQuery(ctx); err != nil {
			return nil, err
		}
		selector := _q.sqlQuery(ctx)
		if err := selector.Err(); err != nil {
			return nil, err
		}
		step := sqlgraph.NewStep(
			sqlgraph.From(clientissuer.Table, clientissuer.FieldID, selector),
			sqlgraph.To(issuer.Table, issuer.FieldID),
			sqlgraph.Edge(sqlgraph.M2O, false, clientissuer.IssuerTable, clientissuer.IssuerColumn),
		)
		fromU = sqlgraph.SetNeighbors(_q.driver.Dialect(), step)
		return fromU, nil
	}
	return query
}

// QueryLcmClient chains the current query on the "lcm_client" edge.
func (_q *ClientIssuerQuery) QueryLcmClient() *LcmClientQuery {
	query := (&LcmClientClient{config: _q.config}).Query()
	query.path = func(ctx context.Context) (fromU *sql.Selector, err error) {
		if err := _q.prepareQuery(ctx); err != nil {
			return nil, err
		}
		selector := _q.sqlQuery(ctx)
		if err := selector.Err(); err != nil {
			return nil, err
		}
		step := sqlgraph.NewStep(
			sqlgraph.From(clientissuer.Table, clientissuer.FieldID, selector),
			sqlgraph.To(lcmclient.Table, lcmclient.FieldID),
			sqlgraph.Edge(sqlgraph.M2O, false, clientissuer.LcmClientTable, clientissuer.LcmClientColumn),
		)
		fromU = sqlgraph.SetNeighbors(_q.driver.Dialect(), step)
		return fromU, nil
	}
	return query
}

// First returns the first ClientIssuer entity from the query.
// Returns a *NotFoundError when no ClientIssuer was found.
func (_q *ClientIssuerQuery) First(ctx context.Context) (*ClientIssuer, error) {
	nodes, err := _q.Limit(1).All(setContextOp(ctx, _q.ctx, ent.OpQueryFirst))
	if err != nil {
		return nil, err
	}
	if len(nodes) == 0 {
		return nil, &NotFoundError{clientissuer.Label}
	}
	return nodes[0], nil
}

// FirstX is like First, but panics if an error occurs.
func (_q *ClientIssuerQuery) FirstX(ctx context.Context) *ClientIssuer {
	node, err := _q.First(ctx)
	if err != nil && !IsNotFound(err) {
		panic(err)
	}
	return node
}

// FirstID returns the first ClientIssuer ID from the query.
// Returns a *NotFoundError when no ClientIssuer ID was found.
func (_q *ClientIssuerQuery) FirstID(ctx context.Context) (id int, err error) {
	var ids []int
	if ids, err = _q.Limit(1).IDs(setContextOp(ctx, _q.ctx, ent.OpQueryFirstID)); err != nil {
		return
	}
	if len(ids) == 0 {
		err = &NotFoundError{clientissuer.Label}
		return
	}
	return ids[0], nil
}

// FirstIDX is like FirstID, but panics if an error occurs.
func (_q *ClientIssuerQuery) FirstIDX(ctx context.Context) int {
	id, err := _q.FirstID(ctx)
	if err != nil && !IsNotFound(err) {
		panic(err)
	}
	return id
}

// Only returns a single ClientIssuer entity found by the query, ensuring it only returns one.
// Returns a *NotSingularError when more than one ClientIssuer entity is found.
// Returns a *NotFoundError when no ClientIssuer entities are found.
func (_q *ClientIssuerQuery) Only(ctx context.Context) (*ClientIssuer, error) {
	nodes, err := _q.Limit(2).All(setContextOp(ctx, _q.ctx, ent.OpQueryOnly))
	if err != nil {
		return nil, err
	}
	switch len(nodes) {
	case 1:
		return nodes[0], nil
	case 0:
		return nil, &NotFoundError{clientissuer.Label}
	default:
		return nil, &NotSingularError{clientissuer.Label}
	}
}

// OnlyX is like Only, but panics if an error occurs.
func (_q *ClientIssuerQuery) OnlyX(ctx context.Context) *ClientIssuer {
	node, err := _q.Only(ctx)
	if err != nil {
		panic(err)
	}
	return node
}

// OnlyID is like Only, but returns the only ClientIssuer ID in the query.
// Returns a *NotSingularError when more than one ClientIssuer ID is found.
// Returns a *NotFoundError when no entities are found.
func (_q *ClientIssuerQuery) OnlyID(ctx context.Context) (id int, err error) {
	var ids []int
	if ids, err = _q.Limit(2).IDs(setContextOp(ctx, _q.ctx, ent.OpQueryOnlyID)); err != nil {
		return
	}
	switch len(ids) {
	case 1:
		id = ids[0]
	case 0:
		err = &NotFoundError{clientissuer.Label}
	default:
		err = &NotSingularError{clientissuer.Label}
	}
	return
}

// OnlyIDX is like OnlyID, but panics if an error occurs.
func (_q *ClientIssuerQuery) OnlyIDX(ctx context.Context) int {
	id, err := _q.OnlyID(ctx)
	if err != nil {
		panic(err)
	}
	return id
}

// All executes the query and returns a list of ClientIssuers.
func (_q *ClientIssuerQuery) All(ctx context.Context) ([]*ClientIssuer, error) {
	ctx = setContextOp(ctx, _q.ctx, ent.OpQueryAll)
	if err := _q.prepareQuery(ctx); err != nil {
		return nil, err
	}
	qr := querierAll[[]*ClientIssuer, *ClientIssuerQuery]()
	return withInterceptors[[]*ClientIssuer](ctx, _q, qr, _q.inters)
}

// AllX is like All, but panics if an error occurs.
func (_q *ClientIssuerQuery) AllX(ctx context.Context) []*ClientIssuer {
	nodes, err := _q.All(ctx)
	if err != nil {
		panic(err)
	}
	return nodes
}

// IDs executes the query and returns a list of ClientIssuer IDs.
func (_q *ClientIssuerQuery) IDs(ctx context.Context) (ids []int, err error) {
	if _q.ctx.Unique == nil && _q.path != nil {
		_q.Unique(true)
	}
	ctx = setContextOp(ctx, _q.ctx, ent.OpQueryIDs)
	if err = _q.Select(clientissuer.FieldID).Scan(ctx, &ids); err != nil {
		return nil, err
	}
	return ids, nil
}

// IDsX is like IDs, but panics if an error occurs.
func (_q *ClientIssuerQuery) IDsX(ctx context.Context) []int {
	ids, err := _q.IDs(ctx)
	if err != nil {
		panic(err)
	}
	return ids
}

// Count returns the count of the given query.
func (_q *ClientIssuerQuery) Count(ctx context.Context) (int, error) {
	ctx = setContextOp(ctx, _q.ctx, ent.OpQueryCount)
	if err := _q.prepareQuery(ctx); err != nil {
		return 0, err
	}
	return withInterceptors[int](ctx, _q, querierCount[*ClientIssuerQuery](), _q.inters)
}

// CountX is like Count, but panics if an error occurs.
func (_q *ClientIssuerQuery) CountX(ctx context.Context) int {
	count, err := _q.Count(ctx)
	if err != nil {
		panic(err)
	}
	return count
}

// Exist returns true if the query has elements in the graph.
func (_q *ClientIssuerQuery) Exist(ctx context.Context) (bool, error) {
	ctx = setContextOp(ctx, _q.ctx, ent.OpQueryExist)
	switch _, err := _q.FirstID(ctx); {
	case IsNotFound(err):
		return false, nil
	case err != nil:
		return false, fmt.Errorf("ent: check existence: %w", err)
	default:
		return true, nil
	}
}

// ExistX is like Exist, but panics if an error occurs.
func (_q *ClientIssuerQuery) ExistX(ctx context.Context) bool {
	exist, err := _q.Exist(ctx)
	if err != nil {
		panic(err)
	}
	return exist
}

// Clone returns a duplicate of the ClientIssuerQuery builder, including all associated steps. It can be
// used to prepare common query builders and use them differently after the clone is made.
func (_q *ClientIssuerQuery) Clone() *ClientIssuerQuery {
	if _q == nil {
		return nil
	}
	return &ClientIssuerQuery{
		config:        _q.config,
		ctx:           _q.ctx.Clone(),
		order:         append([]clientissuer.OrderOption{}, _q.order...),
		inters:        append([]Interceptor{}, _q.inters...),
		predicates:    append([]predicate.ClientIssuer{}, _q.predicates...),
		withIssuer:    _q.withIssuer.Clone(),
		withLcmClient: _q.withLcmClient.Clone(),
		// clone intermediate query.
		sql:       _q.sql.Clone(),
		path:      _q.path,
		modifiers: append([]func(*sql.Selector){}, _q.modifiers...),
	}
}

// WithIssuer tells the query-builder to eager-load the nodes that are connected to
// the "issuer" edge. The optional arguments are used to configure the query builder of the edge.
func (_q *ClientIssuerQuery) WithIssuer(opts ...func(*IssuerQuery)) *ClientIssuerQuery {
	query := (&IssuerClient{config: _q.config}).Query()
	for _, opt := range opts {
		opt(query)
	}
	_q.withIssuer = query
	return _q
}

// WithLcmClient tells the query-builder to eager-load the nodes that are connected to
// the "lcm_client" edge. The optional arguments are used to configure the query builder of the edge.
func (_q *ClientIssuerQuery) WithLcmClient(opts ...func(*LcmClientQuery)) *ClientIssuerQuery {
	query := (&LcmClientClient{config: _q.config}).Query()
	for _, opt := range opts {
		opt(query)
	}
	_q.withLcmClient = query
	return _q
}

// GroupBy is used to group vertices by one or more fields/columns.
// It is often used with aggregate functions, like: count, max, mean, min, sum.
//
// Example:
//
//	var v []struct {
//		ClientID string `json:"client_id,omitempty"`
//		Count int `json:"count,omitempty"`
//	}
//
//	client.ClientIssuer.Query().
//		GroupBy(clientissuer.FieldClientID).
//		Aggregate(ent.Count()).
//		Scan(ctx, &v)
func (_q *ClientIssuerQuery) GroupBy(field string, fields ...string) *ClientIssuerGroupBy {
	_q.ctx.Fields = append([]string{field}, fields...)
	grbuild := &ClientIssuerGroupBy{build: _q}
	grbuild.flds = &_q.ctx.Fields
	grbuild.label = clientissuer.Label
	grbuild.scan = grbuild.Scan
	return grbuild
}

// Select allows the selection one or more fields/columns for the given query,
// instead of selecting all fields in the entity.
//
// Example:
//
//	var v []struct {
//		ClientID string `json:"client_id,omitempty"`
//	}
//
//	client.ClientIssuer.Query().
//		Select(clientissuer.FieldClientID).
//		Scan(ctx, &v)
func (_q *ClientIssuerQuery) Select(fields ...string) *ClientIssuerSelect {
	_q.ctx.Fields = append(_q.ctx.Fields, fields...)
	sbuild := &ClientIssuerSelect{ClientIssuerQuery: _q}
	sbuild.label = clientissuer.Label
	sbuild.flds, sbuild.scan = &_q.ctx.Fields, sbuild.Scan
	return sbuild
}

// Aggregate returns a ClientIssuerSelect configured with the given aggregations.
func (_q *ClientIssuerQuery) Aggregate(fns ...AggregateFunc) *ClientIssuerSelect {
	return _q.Select().Aggregate(fns...)
}

func (_q *ClientIssuerQuery) prepareQuery(ctx context.Context) error {
	for _, inter := range _q.inters {
		if inter == nil {
			return fmt.Errorf("ent: uninitialized interceptor (forgotten import ent/runtime?)")
		}
		if trv, ok := inter.(Traverser); ok {
			if err := trv.Traverse(ctx, _q); err != nil {
				return err
			}
		}
	}
	for _, f := range _q.ctx.Fields {
		if !clientissuer.ValidColumn(f) {
			return &ValidationError{Name: f, err: fmt.Errorf("ent: invalid field %q for query", f)}
		}
	}
	if _q.path != nil {
		prev, err := _q.path(ctx)
		if err != nil {
			return err
		}
		_q.sql = prev
	}
	return nil
}

func (_q *ClientIssuerQuery) sqlAll(ctx context.Context, hooks ...queryHook) ([]*ClientIssuer, error) {
	var (
		nodes       = []*ClientIssuer{}
		withFKs     = _q.withFKs
		_spec       = _q.querySpec()
		loadedTypes = [2]bool{
			_q.withIssuer != nil,
			_q.withLcmClient != nil,
		}
	)
	if _q.withIssuer != nil || _q.withLcmClient != nil {
		withFKs = true
	}
	if withFKs {
		_spec.Node.Columns = append(_spec.Node.Columns, clientissuer.ForeignKeys...)
	}
	_spec.ScanValues = func(columns []string) ([]any, error) {
		return (*ClientIssuer).scanValues(nil, columns)
	}
	_spec.Assign = func(columns []string, values []any) error {
		node := &ClientIssuer{config: _q.config}
		nodes = append(nodes, node)
		node.Edges.loadedTypes = loadedTypes
		return node.assignValues(columns, values)
	}
	if len(_q.modifiers) > 0 {
		_spec.Modifiers = _q.modifiers
	}
	for i := range hooks {
		hooks[i](ctx, _spec)
	}
	if err := sqlgraph.QueryNodes(ctx, _q.driver, _spec); err != nil {
		return nil, err
	}
	if len(nodes) == 0 {
		return nodes, nil
	}
	if query := _q.withIssuer; query != nil {
		if err := _q.loadIssuer(ctx, query, nodes, nil,
			func(n *ClientIssuer, e *Issuer) { n.Edges.Issuer = e }); err != nil {
			return nil, err
		}
	}
	if query := _q.withLcmClient; query != nil {
		if err := _q.loadLcmClient(ctx, query, nodes, nil,
			func(n *ClientIssuer, e *LcmClient) { n.Edges.LcmClient = e }); err != nil {
			return nil, err
		}
	}
	return nodes, nil
}

func (_q *ClientIssuerQuery) loadIssuer(ctx context.Context, query *IssuerQuery, nodes []*ClientIssuer, init func(*ClientIssuer), assign func(*ClientIssuer, *Issuer)) error {
	ids := make([]uint32, 0, len(nodes))
	nodeids := make(map[uint32][]*ClientIssuer)
	for i := range nodes {
		if nodes[i].client_issuer_issuer == nil {
			continue
		}
		fk := *nodes[i].client_issuer_issuer
		if _, ok := nodeids[fk]; !ok {
			ids = append(ids, fk)
		}
		nodeids[fk] = append(nodeids[fk], nodes[i])
	}
	if len(ids) == 0 {
		return nil
	}
	query.Where(issuer.IDIn(ids...))
	neighbors, err := query.All(ctx)
	if err != nil {
		return err
	}
	for _, n := range neighbors {
		nodes, ok := nodeids[n.ID]
		if !ok {
			return fmt.Errorf(`unexpected foreign-key "client_issuer_issuer" returned %v`, n.ID)
		}
		for i := range nodes {
			assign(nodes[i], n)
		}
	}
	return nil
}
func (_q *ClientIssuerQuery) loadLcmClient(ctx context.Context, query *LcmClientQuery, nodes []*ClientIssuer, init func(*ClientIssuer), assign func(*ClientIssuer, *LcmClient)) error {
	ids := make([]uint32, 0, len(nodes))
	nodeids := make(map[uint32][]*ClientIssuer)
	for i := range nodes {
		if nodes[i].client_issuer_lcm_client == nil {
			continue
		}
		fk := *nodes[i].client_issuer_lcm_client
		if _, ok := nodeids[fk]; !ok {
			ids = append(ids, fk)
		}
		nodeids[fk] = append(nodeids[fk], nodes[i])
	}
	if len(ids) == 0 {
		return nil
	}
	query.Where(lcmclient.IDIn(ids...))
	neighbors, err := query.All(ctx)
	if err != nil {
		return err
	}
	for _, n := range neighbors {
		nodes, ok := nodeids[n.ID]
		if !ok {
			return fmt.Errorf(`unexpected foreign-key "client_issuer_lcm_client" returned %v`, n.ID)
		}
		for i := range nodes {
			assign(nodes[i], n)
		}
	}
	return nil
}

func (_q *ClientIssuerQuery) sqlCount(ctx context.Context) (int, error) {
	_spec := _q.querySpec()
	if len(_q.modifiers) > 0 {
		_spec.Modifiers = _q.modifiers
	}
	_spec.Node.Columns = _q.ctx.Fields
	if len(_q.ctx.Fields) > 0 {
		_spec.Unique = _q.ctx.Unique != nil && *_q.ctx.Unique
	}
	return sqlgraph.CountNodes(ctx, _q.driver, _spec)
}

func (_q *ClientIssuerQuery) querySpec() *sqlgraph.QuerySpec {
	_spec := sqlgraph.NewQuerySpec(clientissuer.Table, clientissuer.Columns, sqlgraph.NewFieldSpec(clientissuer.FieldID, field.TypeInt))
	_spec.From = _q.sql
	if unique := _q.ctx.Unique; unique != nil {
		_spec.Unique = *unique
	} else if _q.path != nil {
		_spec.Unique = true
	}
	if fields := _q.ctx.Fields; len(fields) > 0 {
		_spec.Node.Columns = make([]string, 0, len(fields))
		_spec.Node.Columns = append(_spec.Node.Columns, clientissuer.FieldID)
		for i := range fields {
			if fields[i] != clientissuer.FieldID {
				_spec.Node.Columns = append(_spec.Node.Columns, fields[i])
			}
		}
	}
	if ps := _q.predicates; len(ps) > 0 {
		_spec.Predicate = func(selector *sql.Selector) {
			for i := range ps {
				ps[i](selector)
			}
		}
	}
	if limit := _q.ctx.Limit; limit != nil {
		_spec.Limit = *limit
	}
	if offset := _q.ctx.Offset; offset != nil {
		_spec.Offset = *offset
	}
	if ps := _q.order; len(ps) > 0 {
		_spec.Order = func(selector *sql.Selector) {
			for i := range ps {
				ps[i](selector)
			}
		}
	}
	return _spec
}

func (_q *ClientIssuerQuery) sqlQuery(ctx context.Context) *sql.Selector {
	builder := sql.Dialect(_q.driver.Dialect())
	t1 := builder.Table(clientissuer.Table)
	columns := _q.ctx.Fields
	if len(columns) == 0 {
		columns = clientissuer.Columns
	}
	selector := builder.Select(t1.Columns(columns...)...).From(t1)
	if _q.sql != nil {
		selector = _q.sql
		selector.Select(selector.Columns(columns...)...)
	}
	if _q.ctx.Unique != nil && *_q.ctx.Unique {
		selector.Distinct()
	}
	for _, m := range _q.modifiers {
		m(selector)
	}
	for _, p := range _q.predicates {
		p(selector)
	}
	for _, p := range _q.order {
		p(selector)
	}
	if offset := _q.ctx.Offset; offset != nil {
		// limit is mandatory for offset clause. We start
		// with default value, and override it below if needed.
		selector.Offset(*offset).Limit(math.MaxInt32)
	}
	if limit := _q.ctx.Limit; limit != nil {
		selector.Limit(*limit)
	}
	return selector
}

// ForUpdate locks the selected rows against concurrent updates, and prevent them from being
// updated, deleted or "selected ... for update" by other sessions, until the transaction is
// either committed or rolled-back.
func (_q *ClientIssuerQuery) ForUpdate(opts ...sql.LockOption) *ClientIssuerQuery {
	if _q.driver.Dialect() == dialect.Postgres {
		_q.Unique(false)
	}
	_q.modifiers = append(_q.modifiers, func(s *sql.Selector) {
		s.ForUpdate(opts...)
	})
	return _q
}

// ForShare behaves similarly to ForUpdate, except that it acquires a shared mode lock
// on any rows that are read. Other sessions can read the rows, but cannot modify them
// until your transaction commits.
func (_q *ClientIssuerQuery) ForShare(opts ...sql.LockOption) *ClientIssuerQuery {
	if _q.driver.Dialect() == dialect.Postgres {
		_q.Unique(false)
	}
	_q.modifiers = append(_q.modifiers, func(s *sql.Selector) {
		s.ForShare(opts...)
	})
	return _q
}

// Modify adds a query modifier for attaching custom logic to queries.
func (_q *ClientIssuerQuery) Modify(modifiers ...func(s *sql.Selector)) *ClientIssuerSelect {
	_q.modifiers = append(_q.modifiers, modifiers...)
	return _q.Select()
}

// ClientIssuerGroupBy is the group-by builder for ClientIssuer entities.
type ClientIssuerGroupBy struct {
	selector
	build *ClientIssuerQuery
}

// Aggregate adds the given aggregation functions to the group-by query.
func (_g *ClientIssuerGroupBy) Aggregate(fns ...AggregateFunc) *ClientIssuerGroupBy {
	_g.fns = append(_g.fns, fns...)
	return _g
}

// Scan applies the selector query and scans the result into the given value.
func (_g *ClientIssuerGroupBy) Scan(ctx context.Context, v any) error {
	ctx = setContextOp(ctx, _g.build.ctx, ent.OpQueryGroupBy)
	if err := _g.build.prepareQuery(ctx); err != nil {
		return err
	}
	return scanWithInterceptors[*ClientIssuerQuery, *ClientIssuerGroupBy](ctx, _g.build, _g, _g.build.inters, v)
}

func (_g *ClientIssuerGroupBy) sqlScan(ctx context.Context, root *ClientIssuerQuery, v any) error {
	selector := root.sqlQuery(ctx).Select()
	aggregation := make([]string, 0, len(_g.fns))
	for _, fn := range _g.fns {
		aggregation = append(aggregation, fn(selector))
	}
	if len(selector.SelectedColumns()) == 0 {
		columns := make([]string, 0, len(*_g.flds)+len(_g.fns))
		for _, f := range *_g.flds {
			columns = append(columns, selector.C(f))
		}
		columns = append(columns, aggregation...)
		selector.Select(columns...)
	}
	selector.GroupBy(selector.Columns(*_g.flds...)...)
	if err := selector.Err(); err != nil {
		return err
	}
	rows := &sql.Rows{}
	query, args := selector.Query()
	if err := _g.build.driver.Query(ctx, query, args, rows); err != nil {
		return err
	}
	defer rows.Close()
	return sql.ScanSlice(rows, v)
}

// ClientIssuerSelect is the builder for selecting fields of ClientIssuer entities.
type ClientIssuerSelect struct {
	*ClientIssuerQuery
	selector
}

// Aggregate adds the given aggregation functions to the selector query.
func (_s *ClientIssuerSelect) Aggregate(fns ...AggregateFunc) *ClientIssuerSelect {
	_s.fns = append(_s.fns, fns...)
	return _s
}

// Scan applies the selector query and scans the result into the given value.
func (_s *ClientIssuerSelect) Scan(ctx context.Context, v any) error {
	ctx = setContextOp(ctx, _s.ctx, ent.OpQuerySelect)
	if err := _s.prepareQuery(ctx); err != nil {
		return err
	}
	return scanWithInterceptors[*ClientIssuerQuery, *ClientIssuerSelect](ctx, _s.ClientIssuerQuery, _s, _s.inters, v)
}

func (_s *ClientIssuerSelect) sqlScan(ctx context.Context, root *ClientIssuerQuery, v any) error {
	selector := root.sqlQuery(ctx)
	aggregation := make([]string, 0, len(_s.fns))
	for _, fn := range _s.fns {
		aggregation = append(aggregation, fn(selector))
	}
	switch n := len(*_s.selector.flds); {
	case n == 0 && len(aggregation) > 0:
		selector.Select(aggregation...)
	case n != 0 && len(aggregation) > 0:
		selector.AppendSelect(aggregation...)
	}
	rows := &sql.Rows{}
	query, args := selector.Query()
	if err := _s.driver.Query(ctx, query, args, rows); err != nil {
		return err
	}
	defer rows.Close()
	return sql.ScanSlice(rows, v)
}

// Modify adds a query modifier for attaching custom logic to queries.
func (_s *ClientIssuerSelect) Modify(modifiers ...func(s *sql.Selector)) *ClientIssuerSelect {
	_s.modifiers = append(_s.modifiers, modifiers...)
	return _s
}
