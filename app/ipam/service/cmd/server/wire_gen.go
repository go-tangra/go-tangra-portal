// Code generated by Wire. DO NOT EDIT.

//go:generate go run -mod=mod github.com/google/wire/cmd/wire
//go:build !wireinject
// +build !wireinject

package main

import (
	"github.com/go-kratos/kratos/v2"
	"github.com/go-tangra/go-tangra-portal/app/ipam/service/internal/cert"
	"github.com/go-tangra/go-tangra-portal/app/ipam/service/internal/data"
	"github.com/go-tangra/go-tangra-portal/app/ipam/service/internal/server"
	"github.com/go-tangra/go-tangra-portal/app/ipam/service/internal/service"
	"github.com/tx7do/kratos-bootstrap/bootstrap"
)

// Injectors from wire.go:

func initApp(context *bootstrap.Context) (*kratos.App, func(), error) {
	certManager, err := cert.NewCertManager(context)
	if err != nil {
		return nil, nil, err
	}
	entClient, cleanup, err := data.NewEntClient(context)
	if err != nil {
		return nil, nil, err
	}
	auditLogRepo := data.NewAuditLogRepo(context, entClient)
	dnsConfigRepo := data.NewDnsConfigRepo(context, entClient)
	systemService := service.NewSystemService(context, dnsConfigRepo)
	subnetRepo := data.NewSubnetRepo(context, entClient)
	ipScanJobRepo := data.NewIpScanJobRepo(context, entClient)
	subnetService := service.NewSubnetService(context, subnetRepo, ipScanJobRepo)
	vlanRepo := data.NewVlanRepo(context, entClient)
	vlanService := service.NewVlanService(context, vlanRepo)
	deviceRepo := data.NewDeviceRepo(context, entClient)
	deviceService := service.NewDeviceService(context, deviceRepo)
	locationRepo := data.NewLocationRepo(context, entClient)
	locationService := service.NewLocationService(context, locationRepo)
	ipAddressRepo := data.NewIpAddressRepo(context, entClient)
	ipAddressService := service.NewIpAddressService(context, ipAddressRepo, subnetRepo)
	ipScanService := service.NewIpScanService(context, ipScanJobRepo, subnetRepo, ipAddressRepo)
	ipGroupRepo := data.NewIpGroupRepo(context, entClient)
	ipGroupService := service.NewIpGroupService(context, ipGroupRepo)
	hostGroupRepo := data.NewHostGroupRepo(context, entClient)
	hostGroupService := service.NewHostGroupService(context, hostGroupRepo)
	grpcServer := server.NewGRPCServer(context, certManager, auditLogRepo, systemService, subnetService, vlanService, deviceService, locationService, ipAddressService, ipScanService, ipGroupService, hostGroupService)
	scanExecutor := service.NewScanExecutor(context, ipScanJobRepo, subnetRepo, ipAddressRepo)
	app := newApp(context, grpcServer, scanExecutor)
	return app, func() {
		cleanup()
	}, nil
}
